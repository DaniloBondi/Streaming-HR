# .streamlit/secrets.example.toml
# Esempio: salva questo file localmente nella cartella .streamlit (NON committare con token reali)
# Usalo come template per creare .streamlit/secrets.toml o per impostare i Secrets su Streamlit Cloud.

API_TOKEN = "metti-il-tuo-token-qui"
